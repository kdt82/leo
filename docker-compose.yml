version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./outputs:/app/outputs
    environment:
      - LEONARDO_API_KEY=${LEONARDO_API_KEY}
      - BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
    restart: unless-stopped

  frontend:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
    environment:
      - VITE_API_URL=http://localhost:8000/api/v1
    depends_on:
      - backend

# Redis is optional for MVP local execution (we used in-memory queue), 
# but if we wanted robust queue:
#  redis:
#    image: redis:alpine
#    ports:
#      - "6379:6379"
